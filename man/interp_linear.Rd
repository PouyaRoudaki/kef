% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interp_linear.R
\name{interp_linear}
\alias{interp_linear}
\title{Fast Linear Interpolation and Extrapolation with Automatic Method Selection}
\usage{
interp_linear(x, y, xnew, threshold = 10000)
}
\arguments{
\item{x}{Numeric vector of x-values (must be sorted in ascending order).}

\item{y}{Numeric vector of corresponding y-values.}

\item{xnew}{Numeric vector of new x-values where interpolation is required.}

\item{threshold}{Integer specifying the dataset size threshold to switch
from R’s `approx()` to the Rcpp-based implementation. Default is 10,000.}
}
\value{
A numeric vector of interpolated y-values corresponding to `xnew`.
}
\description{
This function performs linear interpolation and extrapolation using either the
base R `approx()` function or a fast C++ implementation via Rcpp, depending
on the size of the dataset.
It automatically selects the most efficient method based on a threshold.
}
\examples{
# Example with small dataset (uses approx())
x <- 1:10
y <- (1:10)^2
xnew <- c(2.5, 4.5, 6.5)
interp_linear(x, y, xnew)

# Example with large dataset (uses Rcpp)
set.seed(42)
x <- sort(runif(1e5, 0, 100))
y <- sin(x)
xnew <- sort(runif(1e4, 0, 100))
interp_linear(x, y, xnew)
}
